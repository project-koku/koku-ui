import React from 'react';

export const styles = {
  container: {
    display: 'flex',
    justifyContent: 'center',
    height: '100vh',
    marginTop: '150px',
  },
} as { [className: string]: React.CSSProperties };
