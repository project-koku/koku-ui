import React from 'react';

export const styles = {
  emptyState: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
  },
  sourceTypeTitle: {
    marginLeft: 20,
    marginTop: 8,
    marginBottom: 8,
  },
} as { [className: string]: React.CSSProperties };
