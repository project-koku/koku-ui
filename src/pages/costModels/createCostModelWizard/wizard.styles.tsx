import { StyleSheet } from '@patternfly/react-styles';

export const styles = StyleSheet.create({
  form: {
    width: '350px',
  },
  textArea: {
    maxWidth: '450px',
    minWidth: '350px',
    minHeight: '75px',
    maxHeight: '150px',
  },
});
