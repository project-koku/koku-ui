import React from 'react';

export const styles = {
  emptyState: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
  },
} as { [className: string]: React.CSSProperties };
