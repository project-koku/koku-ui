import { StyleSheet } from '@patternfly/react-styles';
import { global_BackgroundColor_200 } from '@patternfly/react-tokens';

export const styles = StyleSheet.create({
  costDetailsPage: {
    backgroundColor: global_BackgroundColor_200.var,
    minHeight: '100%',
  },
});
