import * as featureActions from './featureActions';
import { FeatureAction, featureReducer, FeatureState, stateKey as featureStateKey } from './featureReducer';
import * as featureSelectors from './featureSelectors';

export { FeatureAction, featureActions, featureReducer, featureSelectors, FeatureState, featureStateKey };
