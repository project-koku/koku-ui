import { create } from 'styles/stylesheet';

export const classes = {
  nav: 'nav-pf-vertical',
  collapsed: 'collapsed',
};

export const styles = create({
  transition: {
    transition: 'width .2s cubic-bezier(.35,0,.25,1)',
  },
});
