export { default } from './ocpOverviewWidget';
export * from './ocpOverviewWidget';

/**
 * Note, the ocpOverviewWidget uses the translation context, but consuming apps might
 * not have the correct context or translation messages. Translation will not work properly.
 * Either wrap the component in required context or remove the translation. *
 */
