{
  "name": "@koku-ui/onprem-cloud-deps",
  "version": "0.0.1",
  "description": "Library of common components and utilities",
  "repository": "https://github.com/project-koku/koku-ui.git",
  "author": "Red Hat",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": ">=22.20.0",
    "npm": ">=11.6.2"
  },
  "files": [
    "dist",
    "src"
  ],
  "exports": {
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./dist/*.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "prebuild": "tsc --noEmit && rimraf dist",
    "build": "tsc --build && npm run copy:css",
    "check:dependencies": "ncu",
    "check:dependencies:update": "ncu -u",
    "clean": "rimraf dist",
    "copy:css": "rsync -Rurv --include '*/' --include '*.css' --exclude '*' src  dist",
    "dev": "tsc -w",
    "lint": "npm-run-all lint:*",
    "lint:ts": "eslint src",
    "lint:ts:fix": "eslint src --fix",
    "test": "node --experimental-strip-types --test 'test/**/*.test.ts'",
    "test:update": ""
  },
  "dependencies": {
    "@patternfly/patternfly": "6.6.0",
    "@patternfly/react-core": "6.6.0",
    "@scalprum/core": "0.10.1",
    "@scalprum/react-core": "0.13.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
